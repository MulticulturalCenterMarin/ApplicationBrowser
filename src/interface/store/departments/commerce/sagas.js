/* ------------------------- External Dependencies -------------------------- */
import { call, put, fork, takeEvery, take } from 'redux-saga/effects';

/* ------------------------- Internal Dependencies -------------------------- */
import reduxSagaFirebase from 'services/Firebase';

/*--- Redux Actions ---*/ 
import {
  COMMERCE_CHECKOUT_REQUEST,
  COMMERCE_CHECKOUT_SUCCESS,
  COMMERCE_CHECKOUT_FAILURE
} from './actions'


function* commerceCheckout({}) {
  try {

  } catch(e) {
    
  }
}